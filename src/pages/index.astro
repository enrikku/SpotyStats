---
import "../styles/global.css";
import Button from "../components/Button.astro";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>LogIn - Spotify</title>
  </head>

  <body class="bg-[#121212]">
    <div class="grid place-items-center h-screen content-center gap-8">
      <div class="text-center">
        <h1 class="text-4xl md:text-6xl font-bold bg-gradient-to-r from-green-400 to-blue-500 text-transparent bg-clip-text mb-4">
          SpotyStats
        </h1>
        <p class="text-gray-400 text-lg md:text-xl">
          Descubre tus artistas y canciones más escuchadas
        </p>
      </div>

      <div class="flex flex-col items-center gap-6">
        <div class="flex gap-4 items-center">
          <div class="w-12 h-12 rounded-full bg-green-500 animate-pulse"></div>
          <div class="w-12 h-12 rounded-full bg-blue-500 animate-pulse delay-100"></div>
          <div class="w-12 h-12 rounded-full bg-purple-500 animate-pulse delay-200"></div>
        </div>
        <Button>Iniciar sesión con Spotify</Button>
      </div>

      <p class="text-gray-600 text-sm max-w-md text-center px-4">
        Conecta tu cuenta de Spotify para ver estadísticas detalladas sobre tu música favorita
      </p>
    </div>
  </body>

  <script type="module">
    function getCookie(name) {
      const match = document.cookie.match(
        new RegExp("(^| )" + name + "=([^;]+)")
      );
      return match ? match[2] : null;
    }

    const token = getCookie("access_token");

	if (token) {
		window.location.href = "./home";
	}
  </script>
</html>
